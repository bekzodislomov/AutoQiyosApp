<!DOCTYPE html>
<html>
<head>
    <title>Авто-Тиндер</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial;
            background: #f0f0f0;
        }
        .car-card {
            width: 100%;
            height: 100vh;
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
        }
        .car-info {
            background: rgba(0,0,0,0.7);
            color: white;
            padding: 20px;
        }
        .buttons {
            display: flex;
            justify-content: space-around;
            padding: 20px;
            background: white;
        }
        button {
            padding: 15px 30px;
            font-size: 20px;
            border: none;
            border-radius: 50%;
            width: 70px;
            height: 70px;
        }
        .dislike { background: #ff4444; color: white; }
        .like { background: #44ff44; color: white; }
    </style>
</head>
<body>
    <div class="car-card" id="currentCar">
        <div class="car-info">
            <h2 id="carName">Toyota Camry 2020</h2>
            <p id="carDesc">2.5L, автомат, передний привод</p>
        </div>
    </div>
    
    <div class="buttons">
        <button class="dislike" onclick="swipeLeft()">👎</button>
        <button class="like" onclick="swipeRight()">👍</button>
    </div>

    <script>
        // ВАШИ 3 АВТОМОБИЛЯ - ЗАМЕНИТЕ ССЫЛКИ!
        const cars = [
            {
                id: 1,
                name: "Toyota Camry 2020",
                desc: "2.5L, автомат, передний привод", 
                photo: "ВАША_ПЕРВАЯ_ССЫЛКА"
            },
            {
                id: 2, 
                name: "BMW X5 2019",
                desc: "3.0L, полный привод, кожаный салон",
                photo: "ВАША_ВТОРАЯ_ССЫЛКА"
            },
            {
                id: 3,
                name: "Mercedes C-Class 2021", 
                desc: "2.0L, автомат, премиум пакет",
                photo: "ВАША_ТРЕТЬЯ_ССЫЛКА"
            }
        ];

        let currentIndex = 0;
        const likedCars = [];

        function showCar() {
            const car = cars[currentIndex];
            document.getElementById('currentCar').style.backgroundImage = `url(${car.photo})`;
            document.getElementById('carName').textContent = car.name;
            document.getElementById('carDesc').textContent = car.desc;
        }

        function swipeLeft() {
            currentIndex = (currentIndex + 1) % cars.length;
            showCar();
        }

        function swipeRight() {
            likedCars.push(cars[currentIndex]);
            alert(`👍 Добавлено в избранное: ${cars[currentIndex].name}`);
            currentIndex = (currentIndex + 1) % cars.length;
            showCar();
        }

        showCar();
    </script>
</body>
</html>
